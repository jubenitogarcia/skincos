name: Retired duplicate deploy path - CRM Pages reconcile

on:
  workflow_dispatch: {}

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Reject retired deployment path
        run: |
          echo "::error::RETIRED_DUPLICATE_DEPLOY_PIPELINE: use deploy-crm-pages.yml for CRM Pages."
          exit 1
