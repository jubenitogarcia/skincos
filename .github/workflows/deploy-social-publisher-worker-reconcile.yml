name: Retired duplicate deploy path - Social Publisher reconcile

on:
  workflow_dispatch: {}

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Reject retired deployment path
        run: |
          echo "::error::RETIRED_DUPLICATE_DEPLOY_PIPELINE: use deploy-social-publisher-worker.yml for Social Publisher."
          exit 1
