# Addendum de sincronização pública — 2026-08-23

Este addendum complementa `BACKUP-2026-08-22.md`. Depois da captura inicial,
os refs locais recuperáveis foram publicados nos repositórios canônicos sem
force-push.

## Estado verificado

- Skincos: os 1.031 heads locais estão presentes no `origin` (1.131 heads
  públicos após a sincronização). Os 16 nomes que tinham um tip remoto
  divergente foram preservados em
  `backup/preformat-20260823/diverged/<branch-original>`.
- BioEvo: os 110 heads locais estão presentes no `origin` (111 heads públicos).
  O `master` local divergente foi preservado em
  `backup/preformat-20260823/diverged/master`.
- Tags: `git push --dry-run origin --tags` retorna `Everything up-to-date` nos
  dois repositórios.
- Nenhum `main`/`master` remoto foi sobrescrito.

## Retomada em outro computador

```powershell
git clone https://github.com/jubenitogarcia/skincos.git
git clone https://github.com/jubenitogarcia/bioevo.git

cd skincos
git fetch --all --tags
git branch -a

cd ..\bioevo
git fetch --all --tags
git branch -a
```

Os branches de produto preservam os nomes originais. Para comparar um tip
local que divergia do remoto, use o correspondente em
`backup/preformat-20260823/diverged/`; esses refs são pontos de recuperação e
não foram mesclados automaticamente em `main` ou `master`.

O backup privado continua disponível para os estados e evidências que não são
apropriados para um repositório público. Screenshots com PII, logs, bancos,
tokens, cookies, diretórios de dependências e artefatos gerados continuam fora
do GitHub.
