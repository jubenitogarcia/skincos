# Archive

`archive/` is for rollback-only or deprecated material.

- `imported/`: imported historical assets kept for reference
- `deprecated/`: retired code or docs not in the active flow

Nothing under `archive/` should be part of normal build, deploy, or runtime
paths.
