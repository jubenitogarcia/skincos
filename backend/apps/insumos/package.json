{
    "name": "@skincos/insumos-worker",
    "private": true,
    "type": "module",
    "description": "Insumos API (Cloudflare Worker) - incorporated into skincos",
    "scripts": {
        "dev": "wrangler dev --config wrangler.toml",
        "deploy": "wrangler deploy --config wrangler.toml --keep-vars",
        "d1:migrate": "wrangler d1 migrations apply skincos-db --config wrangler.toml",
        "lint": "echo \"(todo)\""
    },
    "devDependencies": {
        "wrangler": "^4.100.0"
    },
    "dependencies": {
        "bcryptjs": "^2.4.3",
        "qrcode-generator": "^1.4.4"
    },
    "pnpm": {
        "overrides": {
            "esbuild": "0.28.1",
            "undici": "7.24.4",
            "ws": "8.21.0"
        }
    }
}
