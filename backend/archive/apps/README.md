Arquivos e serviços legados movidos para `backend/archive/apps`.

Se algo voltar a ser usado, deve ser promovido para `backend/apps/*` (módulo ativo) e referenciado pelos scripts em `backend/scripts/*`.
