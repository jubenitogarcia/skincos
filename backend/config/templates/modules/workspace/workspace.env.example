# Copie para: config/workspace.local.env (não commitado)

# Raiz de dados locais (estado)
VAR_DIR=./var

# CRM (API/Frontend)
CRM_API_PORT=8099
CRM_PORT=5173

# Auth básico (opcional) no CRM: user:pass
# CRM_BASIC_AUTH=admin:admin

# WhatsApp (core integra via URLs)
UNIFIED_SYSTEM_URL=http://localhost:3001
CRM_UNIFIED_API_KEY=unified-dev-key

