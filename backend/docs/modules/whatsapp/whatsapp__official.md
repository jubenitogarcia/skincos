# WhatsApp Official — docs (geradas)

Os arquivos HTML que existiam aqui eram **docs geradas** (ex.: JSDoc) e foram removidos para reduzir o número de arquivos do repo/workspace.

Se precisar regenerar documentação:
- Gere localmente a partir do código fonte em `backend/apps/whatsapp/official/` (biblioteca) e/ou `backend/apps/whatsapp/official-module/` (serviço).
- Alternativa: usar a documentação upstream do `whatsapp-web.js` (quando aplicável).
