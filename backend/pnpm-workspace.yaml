packages:
  # Simple Node apps (no nested workspaces)
  - "apps/api"
  - "apps/insumos"
  - "apps/instagram/module"

  # NOTE:
  # - `apps/agent-zero/` is its own Nx monorepo (npm workspaces).
  # - `apps/actual-server/` declares yarn@4.
  # - `messaging/channels/whatsapp/engine/` is an independent npm project.
  # Keep those managed independently to avoid nested workspace conflicts.
