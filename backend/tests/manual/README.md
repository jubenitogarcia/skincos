Manual/smoke suites vivem aqui.

- `sales_chart_messenger/`: scripts e evidências do módulo Sales Chart Messenger.
- `whatsapp/`: scripts de teste manual do módulo WhatsApp.

Execute localmente apenas em ambientes seguros (pode tocar serviços reais). Use `./backend/scripts/test.sh manual` ou rode scripts específicos conforme README de cada módulo.
