# AI Improve Runner

This document is generated by the AI runner to track small, safe changes associated with issues labeled `ai`.

- The runner plans changes based on labels like `ai:docs`, `ai:ci`, and `ai:refactor-small`.
- It validates using the existing `scripts/ci-smoke-assert.sh` script when available.
- It then creates a branch and PR, and requests auto-merge once checks are green.

You can safely delete these logs later.
