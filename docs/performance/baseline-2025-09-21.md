# Quick Baseline — 2025-09-21

Environment: macOS (local), services assumed running on defaults (API 8099, WA 3001, FE 5173)

Command:

```
./docs/performance/quick-baseline.sh
```

Results:

```
== Health ==
              API health     ~0.95 ms  http://localhost:8099/api/health
               WA health     ~0.37 ms  http://localhost:3001/health
               FE health     ~0.43 ms  http://localhost:5173/health
          FE→WA health     ~0.44 ms  http://localhost:5173/whatsapp/health

== Orchestrator ==
     orchestrator status     ~2.20 ms  http://localhost:8099/api/wa-orchestrator/status
           channels list     ~1.48 ms  http://localhost:8099/api/wa-orchestrator/channels

== Done ==
```

Notes:
- Endpoints responded successfully; latencies are snappy on localhost.
- Keep this as a baseline for Phase 2+ dependency changes.
- For consistency, re-run baseline before and after each optimization PR.
