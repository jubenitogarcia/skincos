# CRM Module

Current layout:

- `web/`: canonical CRM frontend path
- `api/`: canonical CRM backend path
- `docs/` and `ops/`: reserved for module-local guidance

The CRM module now groups both the web and API surfaces under `modules/crm/`.
