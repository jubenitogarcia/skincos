# Meta Ads Module

Canonical home for the Meta Ads surface.

Current layout:

- `meta-ads/`: active workspace for the Meta Ads API, worker, web app, and
  packages

The CRM UI that interacts with this module now lives under `modules/crm/web/`.
