# Ops

`ops/` centralizes cross-module operational helpers that should not live inside
a single product domain.

- `deploy/`: deploy orchestration
- `runtime/`: runtime layouts and host-side guidance
- `bootstrap/`: onboarding/bootstrap helpers
- `checks/`: shared operational checks
