# Orb tasks

The repository root `TASKS.md` is authoritative.

Remaining product follow-up: confirm the Google Calendar OAuth scopes,
`GOOGLE_CALENDAR_ID`, approved test data and a safe validation window before
activating clinic workflows that can create real bookings.

Runtime migration, native release promotion, backup/restore proof and legacy
service retirement are complete. Operational regressions belong in a dedicated
branch/worktree with local and public evidence.
