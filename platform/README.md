# Platform

`platform/` is reserved for shared code that is genuinely reused across
multiple modules.

- `data/`: shared data assets and schemas
- `shared/`: reusable code with proven multi-module use
- `contracts/`: cross-module interfaces and data contracts
- `devtools/`: reusable tooling helpers
- `observability/`: shared monitoring and diagnostics assets
