# NPM Audit Triage (2026-03-04)

## Resumo executivo
- Dependências totais auditadas: `1285`
- Vulnerabilidades (audit completo): `22` (`3 critical`, `8 high`, `1 moderate`, `10 low`)
- Vulnerabilidades em dependências de produção (`npm audit --omit=dev`): `1 high`

## Escopo e comandos
- Audit completo: `npm audit --json > reports/quality/npm-audit-full-2026-03-04.json`
- Audit produção: `npm audit --omit=dev --json > reports/quality/npm-audit-prod-2026-03-04.json`

## Achados principais
1. Produção:
- `next@14.2.35` com `1` advisory `high`.
- Remediação automática indicada pelo npm exige `next@16.1.6` (`semver major`), portanto não aplicada automaticamente nesta rodada.

2. Desenvolvimento/Build toolchain:
- A maior parte dos `critical/high` vem da cadeia transitive de `@opennextjs/cloudflare`.
- O npm sugere upgrade para `@opennextjs/cloudflare@1.17.1`, mas essa versão exige peer de `next` mais novo que `14.2.35`.

## Medidas aplicadas nesta execução
- Pin explícito de dependências críticas de build no `package.json`:
  - `@opennextjs/cloudflare`: `1.15.1`
  - `wrangler`: `4.60.0`
- Motivo: evitar drift de lockfile para combinações incompatíveis e manter builds reproduzíveis até a trilha de upgrade major.

## Plano de mitigação recomendado
1. Abrir trilha de upgrade controlado: `next@14` -> `next@16` e `@opennextjs/cloudflare` compatível.
2. Reexecutar suíte completa (`npm run quality:ci`) em branch de upgrade.
3. Reexecutar `npm audit --omit=dev` e aceitar merge só com `0 high/critical` em produção.
